fastapi>=0.110,<1
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic-settings>=2.2
paddlepaddle>=3.0.0,<4.0
paddleocr>=3.0.0,<4.0
PyMuPDF>=1.24
Pillow>=10
numpy<2
opencv-python-headless>=4.8
# Optional ONNX Runtime backend exposed via the `engine=onnx` request param.
# Uses PP-OCRv4 ONNX models bundled with the package; typically 2-4x faster
# than PaddleOCR on CPU and fully multi-threaded.
rapidocr-onnxruntime>=1.4,<2
